#include "main.h"
/**
* _abs - calculates the absolute value of an integer
* @x: value of function
*
* Description: return function results
*
* Return: interger
*/
int _abs(int x)
{
if (x >= 0)
return (x);
else
return (x * -1);
return (0);
}
